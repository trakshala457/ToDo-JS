# 📝 Todo List App

A simple and interactive **Todo List** built using **HTML**, **CSS**, and **JavaScript**.  
This app allows users to add, delete, and manage their daily tasks efficiently.

---

## 🚀 Features

- ➕ Add new tasks easily
- ❌ Delete tasks using an (x) button
- ✅ Mark tasks as completed (strikethrough)
- ⌨️ Add tasks using the **Enter** key
- ⚠️ Alerts when input is empty
- 🔢 Displays total task count
- 🆔 Each task has a unique ID using **UUID**

---

## 🧩 Technologies Used

- **HTML5** – Structure of the app  
- **CSS3** – Styling and layout  
- **JavaScript (ES6)** – Functionality and interactivity  
- **UUID** – For generating unique task IDs

---

## ⚙️ How It Works

1. Type your task in the input box.
2. Click the **Add** button or press **Enter** to add it.
3. Each task appears in a list with:
   - A checkbox to mark completion
   - A delete (x) button to
